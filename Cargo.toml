[package]
name = "rss2x"
version = "0.1.0"
edition = "2021"

[dependencies]
rss = "2.0"

clap = { version = "4.5", features = ["derive"] }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

html2text = "0.6"
